# 白云 HSD 开发者部落 UI 设计基线

- 状态：强制执行
- 覆盖范围：公开官网、成员任务页、管理端
- 主要验收视口：1440px 桌面端
- 当前实现入口：app/assets/css/main.css
- 产品与权限约定：init/AGENTS.md

## 1. 文档目的

本文件是白云 HSD 开发者部落 Web 项目的 UI 设计入口。新增页面、扩展现有页面、创建组件或调整交互前，必须先阅读本文件。

本文件用于保证后续页面继续属于同一个产品，而不是只复用品牌红的独立页面。设计与实现必须同时维持：

- 品牌一致性。
- 信息层级与页面骨架一致性。
- 公开内容、个人任务和管理任务之间的密度差异。
- 完整的加载、空、错误、成功和权限状态。
- 桌面、平板和手机端的可用性。
- 键盘、焦点、对比度和减少动效支持。

## 2. 文档职责与冲突处理

各文档职责如下：

| 文档 | 负责内容 |
| --- | --- |
| DESIGN.md | UI 风格、设计令牌、页面骨架、组件形态、交互状态、响应式、可访问性和视觉验收 |
| init/AGENTS.md | 品牌身份、产品内容、业务规则、权限边界和设计变更记录 |
| docs/superpowers/specs/ | 单项功能或页面的已确认专项设计 |
| app/assets/css/main.css | 已确认设计在当前代码中的实现 |

冲突按所属领域处理：

1. 用户最新确认的专项设计决策优先。
2. 品牌、内容、业务和权限冲突时，以 init/AGENTS.md 为准。
3. 纯 UI 数值、页面骨架、组件和响应式冲突时，以 DESIGN.md 为准。
4. 既有页面实现只能作为参考，不能覆盖已确认规范。

偏离本文件时，必须先记录偏离原因、影响页面和替代规则，获得确认并更新设计文档后再修改页面。

## 3. 品牌定位

### 3.1 产品身份

- 产品名称：白云 HSD 开发者部落。
- 英文辅助名称：Baiyun HSD Developer Community。
- 品牌口号：鸿蒙启航，共赴星河万里。
- 华为开发者生态和华为 ICT 学院只作为背景或合作体系说明，不替代产品名称。
- 未确认授权前，不使用华为 Logo、Huawei ICT Academy 标识或其他官方视觉资产。

### 3.2 视觉定位

整体定位为：

> 正式科技门户 + 校园创新社区

关键词：

- 科技。
- 权威。
- 克制。
- 校园活力。
- 项目真实感。
- 内容可信度。
- 青年行动力。

视觉必须让学校、老师和合作方感到专业可信，同时让学生感到可以参与、可以成长、可以留下真实作品。

### 3.3 设计原则

- 使用大面积留白、清晰栅格、强标题和明确编号建立秩序。
- 品牌红负责行动和重点，近黑负责技术舞台和强承载区域。
- 正文区域保持浅色、克制和易阅读。
- 优先使用完整横向区段，不以大量悬浮卡片拼接整页。
- 卡片只用于独立、可点击、可比较或需要脱离背景的信息。
- 以真实项目、活动、作品和成员故事建立信任。
- 信息结构先于装饰，交互反馈先于动效。
- 管理端可以提高密度，但不能牺牲层级、可读性和操作安全。

### 3.4 禁止模式

- 不使用紫色或蓝紫渐变作为默认科技风。
- 不使用同质化的三列“图标圆圈 + 标题 + 两行介绍”模板。
- 不把所有模块统一做成大圆角、强阴影或玻璃拟态卡片。
- 不使用装饰气泡、波浪、漂浮圆形填补空白。
- 不使用 Emoji 作为正式图标或页面装饰。
- 不将所有内容居中排版。
- 不使用未经授权的宣传海报、参考截图或随机人物图片。
- 不将桌面页面按比例缩小后直接作为手机页面。
- 不只使用颜色表达状态。

## 4. 共享设计令牌

设计令牌集中定义在 app/assets/css/main.css 的 :root 中。新增颜色、圆角、阴影或全局尺寸前，应先判断能否复用现有令牌。

### 4.1 色彩

| 角色 | 当前值 | 使用场景 |
| --- | --- | --- |
| 品牌红 | #B1202B | 主按钮、激活态、关键编号、重点状态 |
| 品牌深红 | #8F1822 | 品牌红悬停和深色变化 |
| 近黑 | #211F1E | 项目舞台、强调区域、管理侧栏、页脚 |
| 深色卡片 | #302D2A | 深色区域内的分层内容 |
| 暖白 | #F4F0EB | 组织、成员成长和品牌叙事 |
| 冷灰 | #F5F6F7 | 内容列表、信息区和管理工作区背景 |
| 素材位灰 | #DEDAD5 | 未接入正式素材的图片区域 |
| 主要文字 | #1F1D1C | 标题和正文 |
| 次要文字 | #6F6963 | 说明、元数据和辅助信息 |
| 分割线 | #E2DFDB | 列表、区段、表格和内容边界 |
| 表面白 | #FFFFFF | 正文、表格、表单和操作表面 |

使用规则：

- 品牌红用于关键行动，不铺满大量普通内容。
- 深色区域必须使用足够对比度的白色或浅色文字。
- 成功、警告、错误和处理中状态必须同时包含文字或图形标识。
- 页面中出现新的相近灰色或红色前，优先归并到已有令牌。

### 4.2 圆角与阴影

- 基础圆角：4px。
- 中等圆角：8px。
- 普通按钮、输入框、内容块和状态标签使用 4–8px。
- 圆形只用于头像、状态点和确实需要的圆形控件。
- 胶囊形只用于短状态标签，不用于普通容器。
- 正式页面优先通过背景、边框和间距建立层级。
- 阴影主要用于菜单、抽屉、弹窗、灯箱和需要脱离背景的操作面板。
- 普通列表卡片只能使用极弱阴影，不能形成悬浮卡片墙。

### 4.3 内容宽度和基础尺寸

- 官网主要内容宽度：1240px。
- 官网容器：宽度不超过 1240px，桌面左右最少保留 32px。
- 官网导航当前高度：72px。
- 一级页面 Banner 主体最小高度：356px。
- 首页主视觉最小高度：480px。
- 官网标准内容区上下间距：100px。
- 手机标准内容区上下间距：72px。
- 管理端内容宽度：视口内自适应，最大 1480px。
- 管理端桌面内容左右安全间距：28px。
- 官网主要按钮最小高度：48px。
- 管理端紧凑按钮通常为 42–44px。
- 所有触控目标不得小于 44px。

## 5. 排版系统

### 5.1 字体

官网和普通正文使用：

Microsoft YaHei、Noto Sans SC、PingFang SC、sans-serif。

管理端的大数字和部分重点标题允许使用：

Georgia、Times New Roman、Songti SC、serif。

等宽字体只用于代码、文件标识或确实需要等宽对齐的数据，不作为装饰字体。

### 5.2 层级

- 页面只能有一个主要 H1。
- 内容区使用 H2，区内条目或子主题使用 H3。
- 正文桌面端不得小于 16px；管理端高密度辅助信息可以使用 11–14px，但核心操作和正文不能依赖微小字号。
- 正文行高保持约 1.6–1.75。
- 大标题使用紧凑行高和负字距，正文不使用过紧字距。
- 正文每行建议控制在 45–75 个中文字符的可读范围。

### 5.3 Eyebrow

Eyebrow 是当前视觉语言的重要识别元素：

- 使用短英文或英文辅助标签。
- 字号通常为 11–13px。
- 使用品牌红或深色区域中的浅红色。
- 字重较高，字距约 0.14–0.16em。
- 不能代替中文标题或成为唯一页面名称。

## 6. 三套界面语言

### 6.1 公开官网：编辑部式科技门户

公开官网负责品牌、内容和参与入口。

特征：

- 大标题、大面积留白和明确内容区段。
- 横向完整背景区分白、暖白、冷灰、品牌红和近黑。
- 非对称主次布局优先于同质卡片网格。
- 使用编号、日期、细边框和英文 Eyebrow 建立编辑部气质。
- 内容主图、作品和项目素材承担信任建设。
- 图片缺失时使用明确 HSD 素材位，不假装已有正式图片。

### 6.2 成员任务页：品牌化任务界面

成员任务页承载登录、报名、个人空间和结果等操作。

特征：

- 继续使用官网导航、品牌色和 Footer。
- 减少宣传性布局，强调当前状态、下一步和隐私说明。
- 使用任务页头、主操作区、状态面板、表单和辅助侧栏。
- 个人数据只能在明确登录和权限边界内展示。
- 结果、申请和考核状态必须同时说明含义与下一步。

### 6.3 管理端：高密度任务工作台

管理端服务管理员的连续任务和高风险操作。

特征：

- 固定近黑侧边栏、白色顶栏、冷灰工作区。
- 白色数据面板、细边框、小圆角、极弱阴影。
- 大数字和重点标题可以使用衬线字体。
- 表格、筛选、状态标签、详情抽屉和确认层是主要结构。
- 保存、审核、发布和下架必须是不同状态。
- 高风险操作不能在表格行内直接完成。

官网与管理端共享品牌令牌、状态语义、表单原则和可访问性规则，但不强制复用展示组件。

## 7. 公开官网页面骨架

新增公开页面必须选择以下一种主要骨架。

### 7.1 品牌叙事页

适用：部落介绍、中心、招新和需要完整品牌表达的一级页面。

结构：

1. SiteHeader。
2. PageBanner 或经确认的专用品牌首屏。
3. 两个以上具有明确主题的横向内容区段。
4. 参与、浏览或了解更多的行动区。
5. SiteFooter。

### 7.2 内容列表页

适用：项目、活动、画廊、资源和人员目录。

结构：

1. PageBanner。
2. FilterToolbar 或等效筛选和数量区。
3. 列表、网格或主次编排。
4. 筛选无结果状态。
5. 分页或明确的加载策略。
6. SiteFooter。

列表项必须优先回答：这是什么、为什么值得查看、当前状态是什么、点击后去哪里。

### 7.3 内容详情页

适用：项目、活动、资源、成员和媒体专题详情。

结构：

1. PageBanner 或任务型详情页头。
2. 主要内容区。
3. 结构化侧栏或事实摘要。
4. 明确行动入口。
5. 关联内容或返回路径。
6. SiteFooter。

正文和侧栏必须形成主次关系，不能堆叠多个等权卡片。

### 7.4 个人任务页

适用：登录、报名、成员空间和结果中心。

结构：

1. 任务页头或身份上下文。
2. 当前状态和任务说明。
3. 主表单或主要操作区。
4. 隐私、权限和填写帮助。
5. 成功、失败和无权限状态。
6. 明确返回路径。

## 8. 管理端页面骨架

### 8.1 工作台

包含页面标题、核心指标、按风险排序的待办、业务进度、发布动态、存储状态和审计摘要。指标和待办必须可以进入带上下文的业务页面。

### 8.2 列表管理页

统一包含：

- AdminPageHeading。
- 主要新建动作。
- 搜索和组合筛选。
- 结果数量。
- 表格或媒体网格。
- 批量操作。
- 分页。
- 加载、错误、空数据和筛选无结果。

### 8.3 详情抽屉

- 桌面宽度约 560–620px。
- 打开时锁定背景滚动并聚焦关闭按钮。
- 支持 Escape 关闭。
- 关闭后焦点返回触发位置。
- 用于快速查看和轻量编辑。
- 高风险动作必须进入独立确认层。

### 8.4 完整编辑页

适用于项目、活动、新闻、Banner、学习资料、成员资料和权限配置。

- 主区域承载字段表单。
- 辅助区域承载状态、影响范围、预览和发布设置。
- 保存草稿、提交审核、预览和发布必须分开。
- 存在未保存修改时，离开前必须提示。

### 8.5 权限与流程页

权限矩阵、考核流程和发布流程必须显示当前阶段、可执行动作、不可执行原因和影响范围。高风险变化必须二次确认。

## 9. 组件复用

### 9.1 官网组件

优先复用：

- SiteHeader。
- SiteFooter。
- PageBanner。
- FilterToolbar。
- EmptyState。
- MediaPlaceholder。
- HsdAvatar。
- GalleryMediaFrame 和 GalleryLightbox。

### 9.2 管理端组件

优先复用：

- AdminPageHeading。
- AdminStatusPill。
- AdminRecordWorkspace 所体现的筛选、表格、抽屉和确认模式。
- 已有管理端列表、指标、编辑器和状态面板样式。

### 9.3 提取规则

- 同一种结构出现两次，可以暂时保持页面内实现。
- 同一种结构出现三次，或需要统一交互、状态和可访问性时，应提取为组件。
- 组件职责必须单一。
- 组件属性使用业务语义，不通过大量布尔参数拼装互不相关的形态。
- 组件内部实现可以调整，但对外语义和可访问性行为必须稳定。
- 不为了追求复用而强行合并官网展示组件与管理端任务组件。

## 10. 图片、头像与内容

- 正式图片必须为项目单独设计或获得明确授权。
- 不使用需求海报充当 Banner、背景或内容图片。
- 未提供素材时统一使用 MediaPlaceholder 或等效 HSD 品牌素材位。
- 素材位应说明未来需要的内容类型，不能只有空灰框。
- 图片加载失败时原位显示同尺寸 HSD 降级面，保留标题、说明和导航。
- 公开头像只能通过统一公开解析逻辑输出。
- 未上传或选择不公开时使用白底 HSD 默认头像。
- 不在公开页面暴露原始头像地址、联系方式或内部资料。
- 内容应使用具体项目、活动、作品、日期、角色和状态，避免空泛品牌口号填充页面。

## 11. 表单与状态

### 11.1 表单

- 每个字段必须有可见标签。
- Placeholder 只能作为示例，不能代替标签。
- 必填、格式和业务校验应就近显示。
- 错误文案说明如何修正。
- 提交中禁用重复提交并显示进行状态。
- 主按钮使用明确动作，如“提交招新申请”，不只写“提交”。
- 隐私和使用范围在提交前可见。

### 11.2 页面状态

内容页面至少考虑：

- 初始加载。
- 加载失败。
- 无数据。
- 筛选无结果。
- 内容不存在。

个人任务页面至少考虑：

- 未登录。
- 无权限。
- 暂无记录。
- 操作中。
- 操作成功。
- 操作失败。
- 登录后返回原任务。

管理页面至少考虑：

- 初始加载。
- 加载失败。
- 无数据。
- 筛选无结果。
- 编辑中。
- 保存中。
- 保存成功。
- 保存失败。
- 无权限。
- 数据被其他管理员修改。
- 文件处理失败。
- 服务暂不可用。

反馈文案必须说明发生了什么、原因是什么、下一步可以做什么。不能只显示“操作失败”。

### 11.3 高风险确认

发布结果、修改身份、调整中心、修改权限、下架内容、删除或恢复文件等操作必须显示：

- 操作对象。
- 变更前后。
- 影响范围。
- 不可逆或外部可见影响。
- 返回检查和明确确认按钮。

## 12. 动效

- 导航下划线、按钮和状态变化使用约 160–220ms 的短过渡。
- 抽屉、弹窗和灯箱可以使用轻量位移与透明度变化。
- 动效必须服务于状态变化、层级和空间关系。
- 不使用无业务意义的循环动画、滚动视差和大面积漂浮效果。
- 动效不能代替明确文字反馈。
- 必须支持 prefers-reduced-motion。
- 减少动效模式下，非必要动画和过渡应接近即时完成。

## 13. 响应式

### 13.1 官网断点

| 范围 | 规则 |
| --- | --- |
| 1280px 及以上 | 完整桌面布局 |
| 1024–1279px | 紧凑桌面或平板横屏 |
| 768–1023px | 双列、主次布局或折叠导航 |
| 767px 及以下 | 手机单列和任务优先布局 |

当前 CSS 的主要实现断点为 1180px、900px 和 640px。新页面应复用这些断点，除非专项设计证明需要新的全局断点。

手机端必须重新编排：

- 导航折叠为菜单。
- Banner 左右结构改为上下结构。
- 数据指标可改为 2 × 2。
- 列表、表单和主次内容改为单列。
- 行动按钮在空间不足时纵向排列。
- 媒体画廊保留主次关系，不直接缩小桌面网格。
- Footer 按版权、用途声明、帮助入口顺序纵向排列。
- 不得产生非预期的页面整体横向滚动。

### 13.2 管理端

- 主要设计目标为 1440px 和 1366px 桌面端。
- 1180px 以下减少侧栏和内容间距。
- 860px 以下侧栏压缩为窄导航，复杂网格降为双列或单列。
- 580px 以下可以隐藏侧栏，保留核心任务入口。
- 宽表格和权限矩阵允许容器内横向滚动。
- 不把表格压缩到文字和操作不可读。
- 窄屏适配必须优先保证查看、筛选、保存和退出等核心任务。

## 14. 可访问性

- 正文和操作对比度满足 WCAG 2.1 AA。
- 页面保持 H1、H2、H3 的正确层级。
- 所有链接、按钮、表单和自定义控件可用键盘操作。
- 焦点必须清晰可见；当前实现使用 3px 品牌红系轮廓。
- 打开弹窗、抽屉和灯箱后，焦点进入浮层。
- 关闭浮层后，焦点返回原触发位置。
- 浮层打开时按需要锁定背景滚动。
- 图标按钮必须有可访问名称。
- 不使用颜色作为唯一状态表达。
- 错误、成功、空状态和加载变化应提供适当语义反馈。
- 触控目标不得小于 44px。

## 15. 新页面设计与开发流程

每个新页面按以下顺序推进：

1. 确认目标用户、页面目标、公开范围和登录要求。
2. 明确主要内容、主要行动和完成条件。
3. 选择本文件已有页面骨架。
4. 列出正常、加载、空、错误、成功和权限状态。
5. 确定信息层级和桌面布局。
6. 说明平板和手机如何重新编排。
7. 优先组合现有组件。
8. 仅在现有组件确实无法表达时设计新组件。
9. 新增全局视觉值时先更新共享令牌。
10. 实现后同步更新专项设计、需求或变更记录。
11. 执行视觉、响应式、可访问性和自动化验收。

页面设计不能从“需要几张卡片”开始，应从“用户需要理解什么、完成什么、遇到哪些状态”开始。

## 16. 验收清单

### 16.1 通用

- [ ] 产品名称、品牌表达和内容边界正确。
- [ ] 页面属于明确的既有骨架。
- [ ] 主要行动清晰，次要行动不会争夺注意力。
- [ ] 色彩、字体、圆角、阴影和间距符合共享令牌。
- [ ] 没有紫色渐变、玻璃拟态、装饰气泡和无意义卡片堆叠。
- [ ] 标题层级正确，正文可读。
- [ ] 图片经过授权或使用明确 HSD 素材位。
- [ ] 加载、空、错误、成功和权限状态完整。
- [ ] 所有操作可用键盘完成，焦点行为正确。
- [ ] 不只使用颜色表达状态。
- [ ] 未破坏公开内容、个人数据和管理权限边界。

### 16.2 视口

- [ ] 官网 1440px。
- [ ] 官网 1366px。
- [ ] 官网 768px。
- [ ] 官网 390px。
- [ ] 管理端 1440px。
- [ ] 管理端 1366px。
- [ ] 页面没有非预期整体横向滚动。
- [ ] 宽表格只在自己的容器内滚动。

### 16.3 工程验证

- [ ] TypeScript 类型检查通过。
- [ ] 相关单元测试通过。
- [ ] Nuxt 生产构建通过。
- [ ] 适用的 E2E 测试通过，或已记录与页面无关的环境阻断。
- [ ] 重要新页面保留桌面验收截图。
- [ ] 响应式页面至少人工检查一个手机视口。
- [ ] 页面设计或权限变化已更新对应规格和变更记录。

## 17. 当前参考实现

公开官网参考：

- artifacts/hsd-homepage-1440.png
- artifacts/hsd-about-1440.png
- artifacts/hsd-member-results-1440.png
- 设计稿/页面Banner/00-Banner设计总览.png

管理端参考：

- artifacts/admin/hsd-admin-dashboard-1440.png
- artifacts/admin/hsd-admin-recruitment-1440.png
- artifacts/admin/hsd-admin-homepage-config-1440.png
- artifacts/admin/hsd-admin-media-1440.png
- artifacts/admin/hsd-admin-resources-1440.png
- artifacts/admin/hsd-admin-roles-1440.png

参考截图用于理解已确认的层级、密度和构图，不意味着复制页面内容。新页面必须遵守本文件的系统规则，并根据自己的目标、数据和状态形成清晰结构。
